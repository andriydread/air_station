# Development/test dependencies — NOT installed on the Pi.
# The hardware libraries (board, RPi.GPIO, spidev, adafruit-*) are faked in
# tests/conftest.py, so the suite runs on any machine.
pytest
Flask
requests
Pillow
waitress
